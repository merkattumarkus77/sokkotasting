// Shared between e2e/global-setup.ts (which seeds these into the emulator's
// config/appConfig document) and the specs (which log in with them). Values
// are arbitrary — this is the emulator only, never production (CLAUDE.md).
export const E2E_ADMIN_USERNAME = "e2e-admin";
export const E2E_ADMIN_PASSWORD = "e2e-admin-password";
export const E2E_EVENT_PASSWORD = "e2e-event-password";
